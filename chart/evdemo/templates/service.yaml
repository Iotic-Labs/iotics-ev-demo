apiVersion: v1
kind: Service
metadata:
  name: {{ include "evdemo.fullname" . }}
  labels:
    {{- include "evdemo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "evdemo.selectorLabels" . | nindent 4 }}
